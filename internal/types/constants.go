package types

const (
	LetSection Section = "let"
	DoSection  Section = "do"
)

const (
	DoMethod  = "method"
	DoURL     = "url"
	DoParams  = "params"
	DoQuery   = "query"
	DoHeaders = "headers"
	DoBody    = "body"
)
